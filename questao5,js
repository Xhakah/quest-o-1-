function inverterString(str) {
    let invertida = '';
    for (let i = str.length - 1; i >= 0; i--) {
        invertida += str[i];
    }
    return invertida;
}

// Teste com string informada
let string = prompt("Informe uma string para inverter:");
console.log("String invertida:", inverterString(string));